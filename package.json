{
  "name": "aether-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Aether Engine — engine web ultra-realista para jogos de mundo aberto (WebGL2 + three.js) com demo VoxelCraft",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vite build && node --test --test-concurrency=1 'tests/*.test.mjs'",
    "test:only": "node --test --test-concurrency=1 'tests/*.test.mjs'"
  },
  "dependencies": {
    "three": "^0.186.0"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "vite": "^7.1.0"
  }
}
